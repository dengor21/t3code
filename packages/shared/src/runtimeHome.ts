/**
 * Default runtime home directory for this app when T3CODE_HOME is not set.
 *
 * Kept in shared so server and desktop resolve the same location by default.
 */
export const DEFAULT_T3CODE_HOME_DIR_NAME = ".hal";
